<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD config 3.0//EN//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!-- 上面的东西复制即可  -->
<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/database?serverTimezone=GMT"/>  <!-- database是自己的数据库名*********  -->
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="com/mapper/GoodsMapper.xml"/>     <!-- 对应xml文件的全类名，注意要用/。其他内容复制即可  -->
    </mappers>
</configuration>

